<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:paddingBottom="6sp"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="6sp">

    <TextView
        android:id="@+id/name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:gravity="center_vertical"
        android:text="Example task"
        android:textSize="16sp"
        android:layout_toLeftOf="@+id/select_task"
        android:layout_toStartOf="@+id/select_task" />

    <TextView
        android:id="@+id/description"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/name"
        android:ellipsize="marquee"
        android:maxLines="2"
        android:text="This is a max 2 line description.\n Line 2 of description."
        android:textSize="12sp"
        android:layout_toLeftOf="@+id/select_task"
        android:layout_toStartOf="@+id/select_task" />

    <Button
        android:id="@+id/select_task"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:text="@string/select_task"
        android:onClick="selectTaskHandler"/>

</RelativeLayout>